# API base URL for the LAMT backend (Railway service). Required for /live and /admin.
# Set in CI (GitHub Actions deploy.yml) or .env.local during local development.
NEXT_PUBLIC_API_BASE_URL=https://your-service.up.railway.app